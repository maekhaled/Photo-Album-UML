package view;

/**
 * This is the interface that all the controllers in Photos82 implement
 * 
 * @author Seok Yim, Mae Khaled
 *
 */
public interface Controller {
	/**
	 * Sets up the Controller by linking it with the main controller
	 * 
	 * @param mc	MainController instance
	 */
	void setup(MainController mc);
}
